import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingHorizontal: 8,
  },
  item_separator: {
    height: 3,
  },
  flatlist: {
    paddingVertical: 12,
  },
});

export default styles;