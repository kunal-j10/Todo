const { StyleSheet } = require("react-native");

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 8,
  },
  item_separator: {
    height: 30,
  },
});

export default styles;
